"""
This folder contains example of how to use the package `pyzernike`.
It will be push on the git plateform and allows to give an example of the package.

If you have installed the package locally with the command `pip install -e .`, you can directly import `pyzernike` in your code.

You can directly add the example on the documentation of the package by adding the following line in a `rst' file of the documentation:

.. code-block:: rst

    .. literalinclude:: ../examples/example.py

Replace `../examples/example.py` by the relative path from the corresponding `rst` file to the example file.
"""